<style>
    .dropdown-text {
        color: var(--text-main) !important;
    }

    .dropdown-item:hover {
        background-color: var(--input-bg) !important;
    }

    .dropdown-item.dropdown-text:hover {
        color: var(--text-main) !important;
    }

    .dropdown-item.text-danger:hover {
        color: #dc3545 !important;
    }
</style>

<nav class="admin-nav d-flex w-100 justify-content-between align-items-center" style="height: 85px !important; min-height: 85px !important; padding: 0 3.5rem !important; background: var(--card-bg); border-bottom: 1px solid var(--input-border); transition: background-color 0.3s ease, border-color 0.3s ease; box-sizing: border-box;">

    <div class="d-flex align-items-center">
        <a href="#">
            <img id="authLogo" src="/assets/trex-logo-light.png" alt="Logo" style="height: 50px !important; width: auto !important; object-fit: contain;" />
        </a>
    </div>

    <div class="d-flex align-items-center gap-4">

        <div class="theme-toggle-btn" onclick="toggleTheme()" style="cursor:pointer;">
            <i class="bi bi-sun-fill" id="themeIcon" style="font-size: 1.5rem !important; color: var(--text-main);"></i>
        </div>

        <div class="dropdown">
            <div class="d-flex align-items-center gap-2" data-bs-toggle="dropdown" aria-expanded="false" style="cursor: pointer;">
                <i class="bi bi-person-circle" id="profileIcon" style="font-size: 2rem !important; color: var(--text-main); transition: color 0.3s ease;"></i>
            </div>

            <ul class="dropdown-menu dropdown-menu-end shadow-sm" style="background-color: var(--card-bg); border-color: var(--input-border);">

                <li>
                    <a class="dropdown-item d-flex align-items-center gap-3 py-2 dropdown-text" href="#" onclick="showProfile()">
                        <i class="bi bi-person fs-5"></i> Profil
                    </a>
                </li>

                <li>
                    <button class="dropdown-item d-flex align-items-center gap-3 py-2 w-100 text-start dropdown-text" onclick="if(typeof showMentorInfo === 'function') { showMentorInfo(); }" style="background: transparent; border: none;">
                        <i class="bi bi-person-badge fs-5"></i> Mentörüm
                    </button>
                </li>

                <li><hr class="dropdown-divider" style="border-color: var(--input-border);"></li>

                <li>
                    <button class="dropdown-item d-flex align-items-center gap-3 py-2 text-danger w-100 text-start" onclick="logout()" style="background: transparent; border: none;">
                        <i class="bi bi-box-arrow-right fs-5"></i> Çıkış Yap
                    </button>
                </li>
            </ul>
        </div>
    </div>
</nav>